
# Задания

## 1 задание (1 балл)

Чем отличается let от const в JavaScript?

+ [ ] const применяется только к примитивным типам данных, таким как строки и числ
+ [x] сonst не даёт изменить ссылку на переменную, при этом не гарантируя неизменность содержимогo
+ [ ] const обеспечивает лучшую производительность по сравнению с let в современных движках JavaScript
+ [ ] const автоматически помещает значения в кеш процессора для ускорения доступа к ним
+ [ ] const переменные должны быть объявлены до использования, в отличие от let переменных

## 2 задание (1 балл)

Какой из следующих принципов наиболее точно описывает поведение Object.create(null) по сравнению с {}?

+ [ ] Object.create(null) предоставляет расширенный набор методов для работы с объектом по сравнению с литералом {}
+ [ ] Объекты, созданные через Object.create(null), автоматически очищаются сборщиком мусора после использования
+ [ ] При использовании Object.create(null) все свойства объекта становятся недоступны для перечисления
+ [ ] Object.create(null) создает объект с предопределенным набором свойств, в отличие от пустого литерала {}
+ [x] Объект, созданный через Object.create(null), не наследует ничего от Object.prototype, включая методы вроде hasOwnProperty и toString

## 3 задание (1 балл)

Какую функцию можно назвать идемпотентной?

+ [x] Функция должна давать одинаковый результат и побочный эффект при многократном вызове с теми же аргументами.
+ [ ] Функция должна использовать внешние переменные для обеспечения предсказуемого результата
+ [ ] Функция должна иметь побочные эффекты для корректной работы в асинхронной среде
+ [ ] Функция должна изменять входные параметры для достижения желаемого результата
+ [ ] Функция считается идемпотентной, если она возвращает различные значения при каждом вызове

## 4 задание (1 балл)

Какой из следующих принципов наиболее точно описывает назначение never в TypeScript?

+ [ ] never представляет собой тип, который может содержать любое значение, включая null и undefined
+ [ ] never применяется для указания типа возвращаемого значения у асинхронных функций
+ [x] never используется для типизации функций, которые никогда не возвращают значение — либо выбрасывают ошибку, либо работают бесконечно
+ [ ] never используется для обозначения переменных, которые могут принимать любые значения
+ [ ] never и void являются взаимозаменяемыми типами в контексте функций, которые не возвращают значений

## 5 задание (1 балл)

Какой из следующих методов наиболее эффективно предотвращает уязвимости XSS при вставке динамического контента в DOM?

+ [ ] Использование innerHTML с предварительно очищенными данными для защиты от XSS атак
+ [ ] Применение внешних библиотек для автоматического экранирования всех HTML символов
+ [ ] Создание iframe песочницы для изоляции динамического контента от основного DOM
+ [x] Использование textContent/innerText вместо innerHTML при вставке пользовательского ввода
+ [ ] Установка Content-Security-Policy заголовка для предотвращения выполнения вредоносных скриптов

## 6 задание (1 балл)

Какой из следующих механизмов наиболее эффективно предотвращает блокировку загрузки ресурсов из-за синхронных скриптов в `<head>`?

+ [ ] Загрузка скриптов через динамический импорт для ускорения начального времени загрузки страницы
+ [x] Использование async или defer для внешних скриптов, чтобы они не блокировали парсинг HTML
+ [ ] Использование атрибута integrity для проверки целостности внешних скриптов перед выполнением
+ [ ] Применение service workers для кэширования скриптов и уменьшения времени их загрузки
+ [ ] Использование preload директив для предзагрузки критических ресурсов перед парсингом HTML

## 7 задание (1 балл) `- под вопросом`

Назовите ключевое отличие HTTP/1.1 от HTTP/2 ?

+ [ ] HTTP/2 увеличивает количество одновременных запросов за счет расширения размера TCP окна
+ [x] HTTP/2 полностью заменяет HTTPS и обеспечивает шифрование всех передаваемых данных
+ [ ] HTTP/2 использует UDP вместо TCP для уменьшения задержек при передаче данных
+ [ ] HTTP/2 устраняет необходимость в DNS lookups за счет встроенного кэширования записей
+ [ ] HTTP/2 использует бинарный протокол вместо текстового и применяет сжатие заголовков

## 8 задание (1 балл)

Какое основное отличие git rebase от git merge ?

+ [ ] rebase сохраняет линейную историю, но теряет информацию о порядке разработки в параллельных ветках
+ [ ] merge объединяет ветки без создания дополнительных коммитов, а rebase всегда создает коммит слияния
+ [x] rebase переписывает историю коммитов, а merge создаёт новый коммит
+ [ ] merge применяет алгоритм трёхстороннего слияния, в то время как rebase использует последовательное применение патчей
+ [ ] rebase работает только с удаленными ветками, а merge используется для локальных веток

## 9 задание (1 балл)

Чем отличаются window, global и globalThis?

+ [x] window - глобальный объект в браузере, global - глобальный объект в node.js, globalThis - глобальный объект в любом окружении
+ [ ] window - глобальный объект в node.js, global - глобальный объект в браузере, globalThis - специфичный для серверных приложений интерфейс
+ [ ] global и globalThis представляют один и тот же объект, window используется только для графических компонентов интерфейса
+ [ ] window применяется в тестовой среде, global и globalThis используются в production окружении для безопасности
+ [ ] window, global и globalThis являются синонимами и могут использоваться взаимозаменяемо в любом окружении

## 10 задание (1 балл)

Почему (0.1 + 0.2) !== 0.3 ?

+ [ ] Числа с плавающей точкой в JavaScript используют упрощённый формат хранения, который не поддерживает высокую точность вычислений
+ [ ] Операции с десятичными дробями выполняются в отдельном стеке процессора, что приводит к накоплению ошибок округления
+ [x] Сравнение чисел с плавающей точкой осуществляется с погрешностью из-за особенностей двоичной системы счисления
+ [ ] Потому что числа с плавающей запятой в JavaScript хранятся в формате IEEE 754, который не может точно представить некоторые десятичные дроби, такие как 0.1 и 0.2, что приводит к накоплению малых погрешностей при вычислениях
+ [ ] Результат выражения зависит от текущей локали пользователя и настроек региональных стандартов операционной системы

## 11 задание (1 балл)

Что такое "hoisting" в JavaScript и как он работает с function и class?

+ [ ] Все объявления поднимаются полностью, включая let, const, function и class без исключений
+ [ ] Классы и функции инициализируются до объявления, что позволяет вызывать их в любом месте кода
+ [ ] Hoisting применяется только к стрелочным функциям и методам классов, но не к обычным функциям
+ [ ] Переменные let и const поднимаются аналогично var, но инициализируются значением undefined
+ [x] Объявления функций и классов поднимаются целиком, но классы попадают в Temporal Dead Zone до инициализации

## 12 задание (1 балл)

Какой из следующих подходов наиболее эффективно улучшает производительность при работе с большими списками?

+ [ ] Ленивая пагинация с дозагрузкой данных по требованию, когда пользрватель до них долистал
+ [ ] Кэширование элементов списка в памяти с использованием WeakMap для быстрого доступа
+ [x] Virtual scrolling (рендеринг только видимых элементов) и использование key для стабильной идентификации
+ [ ] Применение CSS containment для изоляции расчетов стилей каждого элемента списка
+ [ ] Использование requestAnimationFrame для оптимизации частоты обновления списка

## 13 задание (1 балл)

Что произойдёт при попытке изменить свойство, помеченное как readonly, через индексный доступ?

```typescript
type Point = { readonly x: number; readonly y: number };
const p: Point = { x: 1, y: 2 };
(p as any).x = 3; // ?
```

+ [x] JavaScript-код выполнится, но TypeScript предотвращает прямое присваивание p.x = 3. Приведение к any обходит проверку
+ [ ] TypeScript преобразует readonly свойства в константы времени выполнения, что предотвращает любые изменения значений
+ [ ] Приведение к типу any активирует режим строгой проверки, который усиливает ограничения на изменение свойств объектов
+ [ ] Свойство x будет автоматически заблокировано на уровне движка JavaScript, независимо от используемых типов TypeScript
+ [ ] Операция присваивания будет проигнорирована без ошибок, так как readonly свойства не могут быть изменены в runtime

## 14 задание (1 балл)

Какое значение display делает элемент строчным, но позволяет задавать width и height?

+ [ ] block — создает блочный контейнер, который начинается с новой строки и растягивается на всю ширину родителя
+ [ ] flex — определяет элемент как гибкий контейнер, который управляет расположением своих потомков
+ [ ] inline — устанавливает строчное отображение элемента, которое не позволяет изменять размеры
+ [x] inline-block — создаёт элемент, который обладает смесью признаков блочного и строчного элементов
+ [ ] table — отображает элемент как таблицу, позволяя задавать ширину и высоту ячеек

## 15 задание (1 балл)

Какой механизм JavaScript позволяет объектам "наследовать" свойства и методы друг от друга?

+ [ ] Модульная система (ES Modules) — обеспечивает инкапсуляцию кода и управление зависимостями
+ [x] Цепочка прототипов (prototype chain)
+ [ ] Замыкания (closures) — сохраняют ссылки на переменные внешних функций после их завершения
+ [ ] Деструктуризация — упрощает извлечение значений из объектов и массивов по шаблону
+ [ ] Diamond inheritance — позволяет указывать сразу несколько родителей у одного класса

## 16 задание (1 балл)

Какой из следующих принципов НАИБОЛЕЕ важен при создании функций, которые будут использоваться в цепочке вызовов (например, obj.method1().method2()) ?

+ [ ] Использовать стрелочные функции для сохранения контекста выполнения метода
+ [ ] Возвращать Promise для обеспечения асинхронной обработки вызовов
+ [ ] Хранить состояние в глобальных переменных для доступа из всех методов
+ [ ] Применять замыкания для создания приватных методов, доступных из цепочки вызовов
+ [x] Возвращать this из методов, чтобы обеспечить возможность чейнинга
