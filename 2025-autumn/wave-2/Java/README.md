
# Задания

## 1 задание (1 балл)

Куда пользователи отправляют сообщения в Kafka?

+ [ ] partition
+ [ ] queue
+ [ ] storage
+ [x] topic

## 2 задание (1 балл)

Чем идентифицируется Spring bean?

+ [ ] автоматически сгенерированным UUID
+ [ ] автоувеличивающимся целочисленным ID
+ [ ] ID, полученным на основе его расположения в памяти
+ [x] уникальным строковым именем
+ [ ] именем класса

## 3 задание (1 балл) - несколько вариантов

Какие аннотации используются для создания бинов в Spring?

+ [x] @Component
+ [x] @Service
+ [x] @Repository
+ [x] @Controller
+ [ ] @Autowired
+ [x] @RestController
+ [ ] @Qualifier

## 4 задание (1 балл)

Какой метод вызывает выполнение потока?

+ [ ] process()
+ [ ] launch()
+ [ ] execute()
+ [x] start()
+ [ ] run()

## 5 задание (1 балл)

Что такое context switch в многопоточности?

+ [ ] Способ обновления контекста пользователя при работе с графическим интерфейсом.
+ [ ] Технология сжатия данных в многопоточном программировании
+ [ ] Механизм выделения памяти под новые потоки
+ [x] Процесс сохранения и восстановления состояния потока при переключении между задачами или потоками

## 6 задание (1 балл) - несколько вариантов

Какие состояния может иметь поток во время своего жизненного цикла?

+ [x] New
+ [x] Runnable
+ [ ] Running
+ [x] Blocked
+ [x] Terminated
+ [ ] Suspended

## 7 задание (1 балл)

Какие ограничения могут быть наложены на столбцы?

+ [x] PRIMARY KEY
+ [x] CHECK
+ [x] DEFAULT
+ [x] UNIQUE

## 8 задание (1 балл)

Какие методы оптимизации медленно работающих запросов вы знаете?

+ [x] Использование подходящих индексов
+ [x] Избежание ненужных JOIN и подзапросов
+ [x] Использование эффективных функций и операторов
+ [x] Анализ планов выполнения для выявления узких мест
+ [ ] Добавление большего количества подзапросов
+ [x] Денормализация таблиц

## 9 задание (1 балл)

Что выведется на консоль в результате работы программы?

```java
public class Main
{
	public static void main (String[] args)
	{
		int a = 10;
		change(a);
		print(a);
	}

	static void change (int a)
	{
		a = 20;
	}

	static void print (int a)
	{
		System.out.println(a);
	}
}
```

+ [ ] Ошибка компиляции
+ [ ] Ошибка выполнения
+ [x] 10
+ [ ] 20
+ [ ] 10 или 20

## 10 задание (1 балл) - несколько вариантов

Какие из следующих являются зарезервированными или ключевыми словами в Java?

+ [ ] array
+ [x] goto
+ [x] null
+ [x] int
+ [ ] sizeof

## 11 задание (1 балл) - несколько вариантов

Какие из следующих способов создания массива являются корректными в Java?

+ [x] int myArray[ ] = {1, 3, 5};
+ [ ] int myArray[ ][ ] = {1,2,3,4};
+ [x] int[ ] myArray = new int[ ]{5, 4, 3};
+ [ ] int[ ] myArray = {"1", "2", "3"};

## 12 задание (1 балл)

Что такое "утечка памяти" (memory leak)?

+ [ ] Попытка использовать неинициализированную переменную
+ [x] Потеря доступа к выделенной области памяти без освобождения
+ [ ] Переполнение стека
+ [ ] Ошибка компиляции при нехватке памяти

## 13 задание (1 балл)

Что означает термин "хэш-коллизия"?

+ [ ] Ошибка при сортировке
+ [x] Совпадение хэш-значений для разных ключей
+ [ ] Потеря данных в массиве
+ [ ] Дублирование переменных

## 14 задание (1 балл)

Какой принцип SOLID говорит о том, что класс должен иметь одну причину для изменения?

+ [x] Single Responsibility
+ [ ] Open/Closed
+ [ ] Liskov Substitution
+ [ ] Interface Segregation
+ [ ] Dependency Inversion

## 15 задание (1 балл)

Как называется паттерн, позволяющий отделить интерфейс от реализации?

+ [ ] Factory
+ [ ] Singletone
+ [ ] Adapter
+ [ ] Strategy
+ [x] Bridge

## 16 задание (1 балл)

Что такое стек вызовов (call stack)?

+ [ ] Список всех переменных программы
+ [ ] Хранилище всех подключённых библиотек
+ [x] Структура, отслеживающая активные функции
+ [ ] Кэш инструкций процессора

## 17 задание (1 балл)

```java
interface Foo
{
	int x = 10;
}

public class Test
{
	public static void main (String[] args)
	{
		Foo.x = 20;
		System.out.println(Foo.x);
	}
}
```

+ [ ] Выведет 10
+ [ ] Выведет 20
+ [x] Ошибка компиляции
+ [ ] Ошибка времени выполнения

## 18 задание (1 балл)

```java
public class Test
{
	public static void main (String[] args)
	{
		char c = 65;
		System.out.println("c = " + c);
	}
}
```

+ [ ] Ошибка компиляции
+ [x] "c = A"
+ [ ] Ошибка времени выполнения
+ [ ] "c = 65"

## 19 задание (1 балл)

```java
List<String> list = Arrays.asList("a", "b", "c");
list.add("d");
```

+ [ ] Список увеличится до 4 элементов
+ [x] UnsupportedOperationException
+ [ ] Ошибка компиляции
+ [ ] NullPointerException

## 20 задание (1 балл)

Что выведет данный код?

```java
class Scratch
{
	public static void main (String[] args)
	{
		int yearsMarried = 2;
		switch (yearsMarried)
		{
			case 1 -> System.out.println("papper");
			case 2 -> System.out.println("cotton");
			case 3 -> System.out.println("leather");
			default-> System.out.println("i don't gotta buy that");
		}
	}
}
```

+ [x] cotton
+ [ ] cotton \n leather
+ [ ] cotton \n leather \n i don't gotta buy that
+ [ ] cotton \n i don't gotta buy that

## 21 задание (1 балл) - несколько вариантов

Какие из следующих операторов используются для изменения структуры таблицы?

+ [x] ALTER
+ [x] TRUNCATE
+ [x] DROP
+ [ ] UPDATE
+ [ ] execute

## 22 задание (1 балл)

Какие строки выведет следующий запрос?
(счет начинается с единицы)

```sql
SELECT *
FROM table_name
LIMIT 5 OFFSET 15;
```

+ [ ] с 36 по 40 строку
+ [ ] с 6 по 15 строку
+ [ ] с 5 по 20 строку
+ [x] с 16 по 20 строку
+ [ ] с 5 по 15 строку
+ [ ] ошибка во время выполнения запроса - Limit не может быть меньше Offset

## 23 задание (1 балл)

Что такое синхронизация в контексте многопоточности?

+ [ ] Процесс запуска потоков одновременно
+ [x] Выполнение потоков "по одному" для предотвращения проблем использования общими ресурсами
+ [ ] Метод остановки всех потоков
+ [ ] Способ ускорения выполнения параллельных задач

## 24 задание (1 балл)

Какую роль играет volatile в реализации потокобезопасного ленивого Singleton в Java?

+ [ ] Запрещает переопределение метода getInstance()
+ [x] Обеспечивает видимость изменений между потоками
+ [ ] Обеспечивает доступ к Singleton только из одного потока
+ [ ] Предотвращает создание экземпляра более одного раза

## 25 задание (1 балл)

Что выведет данный код?

```java
public class Main
{
	public static void main (String[] args)
	{
		var set = new HashSet<Integer>();
		Random random = new Random();

		for (int i = 0; i < 100 ; i++)
		{
			set.add(random.nextInt(10));
		}

		System.out.println(set.size());
	}
}
```

+ [ ] Ошибка компиляции
+ [ ] Ошибка выполнения
+ [ ] 0
+ [ ] 10
+ [x] От 1 до 10
+ [ ] 9

## 26 задание (1 балл) - несколько вариантов

Какие уровни изоляции транзакций поддерживаются в Spring?

+ [x] READ_COMMITTED
+ [x] REPEATABLE_READ
+ [x] READ_UNCOMMITTED
+ [ ] SECURED
+ [x] SERIALIZABLE
+ [x] DEFAULT

## 27 задание (1 балл)

Какой компонент Kafka хранит данные?

+ [x] Broker
+ [ ] Consumer
+ [ ] Producer
